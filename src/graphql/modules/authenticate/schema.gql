enum Role {
  USER
  PREMIUM
  ADM
}

input AuthInput {
  email: String!
  password: String!
}

input RoleInput {
  role: Role! = USER
}

type Mutation {
  login(input: AuthInput!): User
  updateRole(input: RoleInput): User
}
